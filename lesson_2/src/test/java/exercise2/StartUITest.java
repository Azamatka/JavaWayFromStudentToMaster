package exercise2;

import exercise2.models.Comment;
import exercise2.models.Item;
import org.junit.Assert;
import org.junit.Test;

import static org.hamcrest.core.Is.is;
import static org.junit.Assert.*;

/**
 * Created by Mur on 08.12.2016.
 */
 public class StartUITest {}
    //@Test
   /* public void getAllComm() {
      StartUI start = new StartUI();
        start.tracker.addApp(new Item("Matt", "is doctor", 21, 1, new Comment[10]));
        start.tracker.addApp(new Item("John", "is driver", 45, 2, new Comment[10]));
        start.tracker.addApp(new Item("Jeff", "is  programmer", 53, 3, new Comment[10]));
        start.tracker.addApp(new Item("George", "is president", 88, 4, new Comment[10]));
        start.tracker.addComm(1, new Comment("новый комментарий"));
        start.tracker.addComm(2, new Comment("новый комментарий1"));
        for (Comment comment : start.getAllComm(2)) {
            if (comment != null) {
                Assert.assertThat(comment.getComment(), is("новый комментарий1"));

            }

        }
    }
}

*/